<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <FrameLayout
        android:id="@+id/frameLayout"
        android:layout_width="160dp"
        android:layout_height="150dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <ImageView
            android:id="@+id/iv_recyclerview_new_music_daily_img"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginHorizontal="10dp"
            android:background="@drawable/fragment_home_album_background"
            android:clipToOutline="true"
            android:scaleType="centerCrop"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:src="@drawable/img_album_exp3" />
        <ImageView
            android:id="@+id/item_new_music_daily_play_btn"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:src="@drawable/nugu_btn_play_32"
            android:layout_gravity="bottom|end"
            android:layout_marginBottom="8dp"
            android:layout_marginEnd="16dp"
            app:tint="@color/white" />
    </FrameLayout>

    <TextView
        android:id="@+id/tv_recyclerview_new_music_daily_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:paddingStart="10dp"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:textSize="18sp"
        android:maxLines="2"
        android:ellipsize="end"
        app:layout_constraintEnd_toEndOf="@id/frameLayout"
        app:layout_constraintStart_toStartOf="@id/frameLayout"
        app:layout_constraintTop_toBottomOf="@id/frameLayout"
        tools:text="Next Level" />

    <TextView
        android:id="@+id/tv_recyclerview_new_music_daily_singer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:paddingStart="10dp"
        android:textSize="15sp"
        app:layout_constraintEnd_toEndOf="@id/tv_recyclerview_new_music_daily_title"
        app:layout_constraintStart_toStartOf="@id/tv_recyclerview_new_music_daily_title"
        app:layout_constraintTop_toBottomOf="@id/tv_recyclerview_new_music_daily_title"
        tools:text="에스파" />

</androidx.constraintlayout.widget.ConstraintLayout>